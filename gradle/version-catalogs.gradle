dependencyResolutionManagement {
    versionCatalogs {
        create("deps") { from(files("versions/deps.versions.toml")) }
        create("config") { from(files("versions/config.versions.toml")) }
    }
}
