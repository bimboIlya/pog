dependencyResolutionManagement {
    versionCatalogs {
        create("libraries") { from(files("versions/libraries.versions.toml")) }
        create("config") { from(files("versions/config.versions.toml")) }
    }
}
